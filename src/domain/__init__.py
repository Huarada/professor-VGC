"""Domain layer.

Pure business entities and contracts. This package MUST NOT import any
external framework, LLM SDK, Node.js bridge, HTTP client or UI toolkit.
It depends only on the standard library and Pydantic (used purely as a
validation/serialization contract at the system boundaries).
"""
